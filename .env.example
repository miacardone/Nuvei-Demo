# Demo console credentials. These defaults are baked into src/lib/auth.ts so the
# demo runs with no setup; override them here for a shared or hosted deployment.
DEMO_USERNAME=NuveiDemo
DEMO_PASSWORD=Changeme123

# Secret used to sign the session cookie. Set a random value anywhere this is
# reachable by anyone other than you.
AUTH_SECRET=
