# Copy to .env.local. This console is a static SPA with no server, so these
# values are compiled into the JavaScript bundle and are readable by anyone on
# the deployed site. The gate stops a link being wandered into; it is not
# security, and no real credential belongs here.
VITE_DEMO_USERNAME=NuveiDemo
VITE_DEMO_PASSWORD=
